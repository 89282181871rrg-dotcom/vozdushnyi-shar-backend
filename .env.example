# Профиль docker берёт настройки БД отсюда.
# Скопируйте в .env и поправьте при необходимости.
# Файл .env в репозиторий не попадает — он в .gitignore.
DB_HOST=postgres
DB_PORT=5432
DB_NAME=balloon
DB_USER=balloon
DB_PASSWORD=balloon
